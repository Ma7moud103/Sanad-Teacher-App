# 📚 Sanad Teacher App

The **Sanad Teacher App** is a front-end web application built to support teachers with digital tools for managing lessons, students, or school-related activities.  
it’s a **React + Tailwind CSS**-based application, designed with a clean and modern UI and prepared for localization (multi-language support).

---

## ⚙️ Technologies Used

- **React.js** – Core front-end framework  
- **Tailwind CSS** – Styling and layout framework  
- **React Router (most likely)** – For routing between pages  
- **i18next / localization files** – For language translation  
- **Environment Variables (.env)** – To store API endpoints and private keys  
- **JSON data (cities.json)** – For managing location-based data (like city names or regions)

---

## 📂 Project Structure

| Folder / File | Description |
|----------------|-------------|
| `src/` | Contains all React components, pages, and logic |
| `templates/` | Likely holds pre-made templates for layout or UI sections |
| `cities.json` | Stores location data (list of cities) |
| `.env` | Environment variables for API URLs, keys, etc. |
| `tailwind.config.js` | Tailwind CSS configuration file |
| `.gitignore` | Defines ignored files and folders |
| `package.json` | Lists dependencies, scripts, and project metadata |

---

## 💡 What the App Likely Does

- Provides a dashboard/interface for **teachers**.  
- Manages **content**, **students**, or **courses**.  
- Supports **multi-language** environments.  
- Uses **API connections** (possibly via `.env` variables).  
- Has a responsive and modern design built with **Tailwind CSS**.  
